# Normalize line endings so the repo is identical on every OS.
* text=auto eol=lf

*.png binary
*.jpg binary
*.gif binary
*.svg text

# Language stats: don't count generated/vendored files.
clarion/rubric.yaml linguist-generated=true
docs/foundations/** linguist-documentation=true
